// Task: Write a function to determine if an array can be partitioned into two subsets
// such that the sum of elements in both subsets is equal.

function canPartition(nums) {
    // Your code here
}

// Test cases
console.log(canPartition([1, 5, 11, 5])); // Expected output: true
console.log(canPartition([1, 2, 3, 5]));  // Expected output: false
console.log(canPartition([2, 2, 3, 5]));  // Expected output: false
console.log(canPartition([]));            // Expected output: false
