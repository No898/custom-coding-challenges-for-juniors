// Task: Write a function to rotate an array to the right by `k` positions.

function rotateArray(nums, k) {
    // Your code here
    return nums; // Modify this line as needed
}

// Test cases
console.log(rotateArray([1, 2, 3, 4, 5], 2));   // Expected output: [4, 5, 1, 2, 3]
console.log(rotateArray([1, 2, 3], 4));         // Expected output: [3, 1, 2]
console.log(rotateArray([], 1));                // Expected output: []
