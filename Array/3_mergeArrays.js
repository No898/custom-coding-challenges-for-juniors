// Task: Write a function that takes two arrays, merges them,
// and returns a new array without duplicates.

function mergeArrays(arr1, arr2) {
    // Your code here
}

// Test cases
console.log(mergeArrays([1, 2, 3], [2, 3, 4]));    // Expected output: [1, 2, 3, 4]
console.log(mergeArrays([], [1, 2]));              // Expected output: [1, 2]
console.log(mergeArrays([5, 5], [5]));             // Expected output: [5]
