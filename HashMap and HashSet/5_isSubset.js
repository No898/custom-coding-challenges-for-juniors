// Task: Write a function to check if one array is a subset of another.

function isSubset(arr1, arr2) {
    // Your code here
}

// Test cases
console.log(isSubset([1, 2, 3], [1, 2]));   // Expected output: true
console.log(isSubset([1, 2], [1, 2, 3]));   // Expected output: false
console.log(isSubset([], [1, 2, 3]));       // Expected output: true
console.log(isSubset([1, 2], []));          // Expected output: false
